API_TOKEN=changeme
YTDL_SUB_API_URL=http://ytdl-sub-api:5000
YTDL_SUB_API_TOKEN=changeme
TZ=America/New_York
POLL_INTERVAL_MINUTES=30
POLL_LIMIT=30
# YouTube Data API v3 key. Currently optional (used only to fetch video
# descriptions for webhook payloads). Becomes REQUIRED in an upcoming
# release — wytchr is moving off ytdl-sub-api onto the YouTube Data API
# for channel resolution and upload polling.
# Get one at https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=
