[pytest]
testpaths = tests
asyncio_mode = auto
# Each test builds its own app context via the `client` fixture, so the
# default function-scoped event loop is what we want.
asyncio_default_fixture_loop_scope = function
